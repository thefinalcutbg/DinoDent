PRAGMA user_version=40;
CREATE TABLE declaration_template (rowid INTEGER NOT NULL PRIMARY KEY, practice_rzi VARCHAR NOT NULL REFERENCES practice ON DELETE CASCADE ON UPDATE CASCADE, header VARCHAR, subheader VARCHAR, body VARCHAR, footer VARCHAR);

