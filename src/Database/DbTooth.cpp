#include "DbTooth.h"

ProcedureHistory DbTooth::getHistory(std::string patientId, int tooth)
{
    ProcedureHistory history;

    std::string query =
        "SELECT tooth, ";




        return history;
}
